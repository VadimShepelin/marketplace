services:
  redis:
    image: redis:latest
    container_name: redis
    restart: always
    ports:
      - "6378:6379"
    volumes:
      - redis_data:/data
    environment:
      - ALLOW_EMPTY_PASSWORD=yes

volumes:
  redis_data:
